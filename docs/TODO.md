# TODO
- [x] Сделать контрол выбора из малого кол-ва вариантов: radiobuttons based on buttons
  - [x] Добавить поддержку defaultValue, customValue
  - [x] Перевести на него:
    - Basics options - смотри в других режимах (Web UI, OBS dock, Simple mode), подбери 2-4 варианта для кнопок + кнопка `...` - открывает input для ввода значения
    - Silent speed (1, 2, 5, 10), дальше кнопка `...` - открывает input для ввода значения
  - [x] Sounded speed (1, 1.3, 1.5, 2), дальше кнопка `...` - открывает input для ввода значения
  - [x] Silent threshold, значение - подсказка tooltip кнопки, `...`:
    - 0.01 - никогда не вырезает голос, но глушит только тишину на хорошем микрофоне
    - 0.03 - подходит для большинства случаев, для видео с телефона, но может вырезать тихий голос
    - 0.05 - агрессивно вырежет
    - 0.10 - последний разумный предел, до которого можно безболезненно вырезать тишину
  - Video codec: вынести из скобок в tooltip кнопки, галочку Add codec suffix - оставить справа от кнопок
  - Processing mode: local/remote, когда выбран remote, показывать статус соединения `Server <ip> is ready`. Server URL показывать справа от кнопок, только если выбран remote
  - Theme
- [x] Настройки должны стать более компактными, и в то же время понятными, однотипные настройки должны быть рядом, где-то в 1 строку.
- [x] добавь в подсказку к threshold ссылку на статью -
  https://telegra.ph/How-hard-can-you-trim-silence-before-speech-to-text-breaks-08-03

## Доработки контрола после первого прогона

- [x] Инлайн-ввод произвольного значения должен быть той же ширины, что кнопка `…`,
  которую он замещает — сейчас ряд перекомпоновывается прямо во время редактирования.
- [x] Threshold: ссылку на статью перенести в label настройки, убрать отдельную
  кнопку `?` из ряда значений.
- [x] Threshold: разрешить вводить любое значение, но с максимумом `0.9` вместо `1.0`.
- [x] Пресеты показать тоже кнопками (`SegmentedChoice`) вместо `ttk.Combobox`: ширина
  по содержимому плюс отступы 10, вариант **Custom** выбирается сам, когда живые
  значения не совпадают ни с одним пресетом. Сделано для Advanced-строки; Simple mode
  оставлен на `ttk.Combobox` — в окне 470px ряд кнопок с именами пресетов не помещается.