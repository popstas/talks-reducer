# TODO

- [x] Add "mp3" option to the codec selector; when selected, save the result as an mp3 file
- [x] Make menu bar icon on macOS monochrome
- [ ] Launch the macOS app in `server-tray` mode from the regular app icon (not the terminal): add a setting in the main app to enable server mode and minimize to the tray, so it no longer requires the pip CLI command
